query Products($first: Int!, $skip: Int!, $orderBy: ProductOrderByInput!) {
  productsConnection(first: $first, skip: $skip, orderBy: $orderBy) {
    ...ProductList
  }
}

query ProductsPosts {
  posts(where: { displayLocations_contains_all: products }) {
    ...Post
  }
}
